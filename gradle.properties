# Mod metadata
mod_name=ZombiesHelper
mod_id=zombieshelper
mod_version=4.3
mod_archives_name=ZombiesHelper

# Gradle Configuration -- DO NOT TOUCH THESE VALUES.
polyfrost.defaults.loom=3
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureoncommand=true
org.gradle.parallel.threads=4
org.gradle.jvmargs=-Xmx2G

# The 1.8.9 compile toolchain requires JDK 8. Gradle will auto-detect an installed
# JDK 8, or point it at one via `org.gradle.java.installations.paths` in your global
# ~/.gradle/gradle.properties (kept out of the repo so it stays machine-independent).
